name: laravel-dev

services:

  # ---------------------------------------------------------------------------
  # FrankenPHP — Laravel application server
  # ---------------------------------------------------------------------------
  app:
    image: dunglas/frankenphp:latest-php8.5
    working_dir: /app
    volumes:
      - .:/app
      - ./docker/frankenphp/Caddyfile.dev:/etc/caddy/Caddyfile:ro
    ports:
      - "80:80"
      - "443:443"
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_URL: https://${APP_HOST:-mrifqyabdallah.local}
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-mrifqyabdallah}
      DB_USERNAME: ${DB_USERNAME:-laravel}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      VITE_DEV_SERVER_URL: http://${APP_HOST:-mrifqyabdallah.local}:5173
      APP_HOST: ${APP_HOST:-mrifqyabdallah.local}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - laravel

  # ---------------------------------------------------------------------------
  # Vite — frontend dev server with HMR
  # ---------------------------------------------------------------------------
  vite:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev"
    networks:
      - laravel

  # ---------------------------------------------------------------------------
  # PostgreSQL
  # ---------------------------------------------------------------------------
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${DB_DATABASE:-mrifqyabdallah}
      POSTGRES_USER: ${DB_USERNAME:-laravel}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-laravel}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - laravel

networks:
  laravel:
    driver: bridge

volumes:
  postgres_data:
